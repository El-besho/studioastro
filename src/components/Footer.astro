---
import { Facebook, Instagram, Twitter } from 'lucide-react';
import { Logo } from './logo';
import { footerLinks } from '../config/nav';
---

<footer class="bg-muted/50 text-right">
  <div class="container py-12 px-4 md:px-6">
    <div class="grid gap-8 md:grid-cols-4">
      <div class="space-y-4 md:col-span-1">
        <div class="flex justify-end">
         <Logo />
        </div>
        <p class="text-sm text-muted-foreground">
          منصة موثوقة لجميع خدمات منزلك وعملك في السعودية.
        </p>
      </div>
      <div class="grid grid-cols-2 md:grid-cols-3 md:col-span-3 gap-8">
        <div>
          <h3 class="font-headline font-semibold mb-4">أهم الخدمات</h3>
          <ul class="space-y-2">
            {footerLinks.services.map((link) => (
              <li key={link.href}>
                <a href={link.href} class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
        <div>
          <h3 class="font-headline font-semibold mb-4">عن الشركة</h3>
          <ul class="space-y-2">
            {footerLinks.company.map((link) => (
              <li key={link.href}>
                <a href={link.href} class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
        <div>
          <h3 class="font-headline font-semibold mb-4">قانوني</h3>
          <ul class="space-y-2">
            {footerLinks.legal.map((link) => (
              <li key={link.href}>
                <a href={link.href} class="text-sm text-muted-foreground hover:text-foreground transition-colors">
                  {link.label}
                </a>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </div>
    <div class="mt-8 border-t pt-8 flex flex-col sm:flex-row-reverse justify-between items-center gap-4">
      <p class="text-sm text-muted-foreground">
        © {new Date().getFullYear()} إنقاذ. جميع الحقوق محفوظة.
      </p>
      <div class="flex gap-4">
        <a href="https://twitter.com/enqaz" target="_blank" rel="noopener noreferrer" class="text-muted-foreground hover:text-foreground">
          <Twitter class="h-5 w-5" />
        </a>
        <a href="https://facebook.com/enqaz" target="_blank" rel="noopener noreferrer" class="text-muted-foreground hover:text-foreground">
          <Facebook class="h-5 w-5" />
        </a>
        <a href="https://instagram.com/enqaz" target="_blank" rel="noopener noreferrer" class="text-muted-foreground hover:text-foreground">
          <Instagram class="h-5 w-5" />
        </a>
      </div>
    </div>
  </div>
</footer>